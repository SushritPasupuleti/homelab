[project]
name = "homelab"
version = "0.1.0"
description = "Kubernetes homelab stack and deployment tooling"
requires-python = ">=3.11.9,<3.12"
dependencies = [
  "pyyaml>=6.0.2",
]

[tool.uv]
package = false

[build-system]
requires = ["hatchling>=1.18.0"]
build-backend = "hatchling.build"
